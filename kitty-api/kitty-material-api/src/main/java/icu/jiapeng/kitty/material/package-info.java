/**
 * kitty-material-api：按业务域分包（如 {@code catalog}、{@code resource}），域内常见子包 —
 * {@code api}（OpenFeign {@code @FeignClient} + MVC 映射契约）、{@code dto}、{@code vo}、{@code constants} 等；与 {@code kitty-material-func} 对齐。
 */
package icu.jiapeng.kitty.material;
