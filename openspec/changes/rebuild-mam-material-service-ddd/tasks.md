## 1. 基线与工程分层落地

- [ ] 1.1 盘点并标记 `material-func` 历史业务代码的替换边界，形成可删除/可迁移清单（不改代码，仅清单）
- [ ] 1.2 创建 material 新分层目录骨架（api/exchange、application、domain、infra、repo）并补齐模块说明
- [ ] 1.3 建立分层依赖约束与命名规范（禁止 domain 依赖 infra/repo）
- [ ] 1.4 建立异常 DTO 与全局异常映射约定文档（messageSource 国际化键规范）
- [ ] 1.5 建立接口权限注解使用约定（默认走 SaCheckPermission，公开接口白名单机制）

## 2. 数据模型与持久化基础

- [ ] 2.1 设计栏目、资源、存储记录、权限、任务、编目、审核、站内信等表结构草案与索引清单
- [ ] 2.2 明确公共语义字段规则（`public`、`parentId=0`）并形成统一字段字典
- [ ] 2.3 落地 repo 实体与 mapper（MyBatis Plus 注解仅在 repo 层）
- [ ] 2.4 建立资源类型枚举与文件夹资源化持久化约束
- [ ] 2.5 建立编目模板-字段独立绑定表与排序字段
- [ ] 2.6 建立编目实例 EAV 存储表与 `last/version` 版本字段

## 3. 栏目树与权限领域能力

- [ ] 3.1 实现栏目聚合基础操作（新增、重命名、移动、排序）及领域校验
- [ ] 3.2 实现个人栏目虚拟根 `pri_用户id` 的树构建与展示逻辑
- [ ] 3.3 实现私有根子栏目移动约束（禁止跨私有根与普通栏目）
- [ ] 3.4 实现角色权限、`public=true`、`public=false` 三态解析规则
- [ ] 3.5 实现栏目权限缓存读写与精准失效事件
- [ ] 3.6 完成栏目树与权限组合查询接口（含缓存命中回退）

## 4. 资源管理与文件夹结构能力

- [ ] 4.1 实现资源聚合创建与更新（`catalogId` 必填、`parentId=0` 默认）
- [ ] 4.2 实现资源文件夹创建、嵌套与路径重建能力
- [ ] 4.3 实现目录结构保真的文件夹上传入口编排
- [ ] 4.4 实现资源文件指纹（文件大小 + 分段 CRC）计算与存储
- [ ] 4.5 实现资源秒传/排重前置校验流程（基于指纹）
- [ ] 4.6 完成资源查询接口（DB 主路径，不依赖 ES）

## 5. 异构存储与分片上传能力

- [ ] 5.1 定义存储端口接口与存储工厂路由契约
- [ ] 5.2 实现磁盘存储驱动（统一对象键语义）
- [ ] 5.3 实现 S3 存储驱动与基础连通性校验
- [ ] 5.4 实现文件记录与存储记录关联写入流程
- [ ] 5.5 实现分片上传会话模型（创建、续传、完成、取消）
- [ ] 5.6 实现 `Content-Length`/`Content-Range` 头解析与合法性校验
- [ ] 5.7 实现分片并发控制（分布式锁 + 幂等去重）
- [ ] 5.8 实现分片合并与完整性校验（覆盖范围、指纹一致性）

## 6. 用户/权限/配置中心适配

- [ ] 6.1 定义用户上下文端口接口（当前用户、角色、权限集）
- [ ] 6.2 接入 sa-token + kitty-user grpc 插件默认实现
- [ ] 6.3 定义配置中心端口接口（按命名空间读取配置）
- [ ] 6.4 接入 kitty-user grpc 配置读取默认实现
- [ ] 6.5 完成接口权限拦截与业务权限校验联动

## 7. 编目与版本管理能力

- [ ] 7.1 实现编目模板 CRUD 与按栏目/资源类型绑定能力
- [ ] 7.2 实现模板字段定义（输入方式、可选值范围、字段 code）管理
- [ ] 7.3 实现编目实例保存流程（按模板字段校验并写入 EAV）
- [ ] 7.4 实现最新编目查询（`last=true`）
- [ ] 7.5 实现历史版本查询（按版本上界检索 `version<=N`）
- [ ] 7.6 完成编目保存与资源详情联动展示接口

## 8. 转码策略与资源任务中心

- [ ] 8.1 定义转码平台适配端口与任务下发契约
- [ ] 8.2 接入 kitty-transcoder grpc 默认实现
- [ ] 8.3 实现转码策略管理（平台标识、参数、启停状态）
- [ ] 8.4 实现栏目绑定转码策略与上传后优先匹配规则
- [ ] 8.5 实现资源任务统一模型（转码/AI 审核/AI 标签）
- [ ] 8.6 实现任务列表永久保留查询接口
- [ ] 8.7 实现转码失败重试（主记录不变、旧执行态逻辑删除、外部任务 id 更新）

## 9. 检索同步与向量化能力

- [ ] 9.1 定义事务后领域事件与分布式队列消息模型
- [ ] 9.2 实现资源与编目变更后消息投递（提交后触发）
- [ ] 9.3 实现 ES 同步消费者（全量 doc 与字段 update 双消息类型）
- [ ] 9.4 实现向量化端口与来源标识模型（多来源并存）
- [ ] 9.5 实现 `NONE` 空向量安全实现（流程可跑通）
- [ ] 9.6 实现向量数据 MySQL 持久化与 ES8 向量字段写入
- [ ] 9.7 实现检索路由策略（普通查询走 DB，全文/向量走 ES）

## 10. 站内信与通用审核流

- [ ] 10.1 实现站内信领域模型（发送者、接收者、内容、状态、时间）
- [ ] 10.2 实现 SSE 连接管理与按用户通道推送
- [ ] 10.3 实现跨 Pod 队列广播分发逻辑
- [ ] 10.4 实现通用审核流程（提交、审核通过、审核拒绝、结果查询）
- [ ] 10.5 实现资源详情页任务状态聚合视图（转码/审核/标签）

## 11. API 契约与前端联调接口

- [ ] 11.1 实现 material exchange DTO 全量定义（请求/响应分离）
- [ ] 11.2 实现 controller 注解迁移（Exchange 注解到 MVC 注解）
- [ ] 11.3 完成 swagger 与 validation 注解补齐（仅 api/application 层）
- [ ] 11.4 完成栏目树、资源、编目、任务、策略、审核、消息接口路由定义
- [ ] 11.5 完成异常 DTO 与错误码字典对外文档

## 12. 缓存、分布式稳定性与观测

- [ ] 12.1 定义缓存端口接口与默认 Redisson 实现
- [ ] 12.2 建立栏目树、栏目权限、存储元数据缓存策略
- [ ] 12.3 定义分布式锁端口与默认 Redisson 锁实现
- [ ] 12.4 为分片上传、目录迁移、任务重试等关键场景接入锁
- [ ] 12.5 补齐关键链路日志与审计字段（上传、转码、权限拒绝、审核）

## 13. 测试与验收

- [ ] 13.1 为领域核心规则补齐单元测试（public false、pri_根、parentId=0）
- [ ] 13.2 为分片上传并发、重试幂等补齐集成测试
- [ ] 13.3 为 ES 同步与队列重放补齐集成测试
- [ ] 13.4 为站内信跨 Pod 推送补齐联调测试
- [ ] 13.5 为审核流与任务中心补齐端到端测试
- [ ] 13.6 输出迁移演练记录与回滚演练记录
