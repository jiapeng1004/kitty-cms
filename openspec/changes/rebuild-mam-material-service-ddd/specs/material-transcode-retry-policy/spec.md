## ADDED Requirements

### Requirement: 转码失败重试必须保留主记录并逻辑删除历史态
系统 MUST 在转码重试时保持任务主记录 id 不变，对旧任务状态进行逻辑删除，并在外部平台返回任务标识时更新外部任务 id。

#### Scenario: 转码失败后重试
- **WHEN** 某资源转码任务失败且用户触发重试
- **THEN** 系统 SHALL 复用原任务主记录并生成新的有效执行态，同时标记旧执行态为逻辑删除
