## ADDED Requirements

### Requirement: 栏目树必须支持普通栏目与个人虚拟根
系统 MUST 支持普通栏目树与个人栏目虚拟根 `pri_用户id`，并保证个人根可在树接口返回、虚拟根不可排序、其子节点可排序且不可跨私有根非法移动。

#### Scenario: 查询个人栏目树
- **WHEN** 用户请求包含个人栏目能力的栏目树
- **THEN** 系统 SHALL 返回 `pri_用户id` 虚拟根节点及其子栏目，并禁止将其子栏目移动到其他私有根或普通栏目
