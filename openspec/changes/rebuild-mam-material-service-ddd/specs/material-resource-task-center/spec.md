## ADDED Requirements

### Requirement: 资源任务中心必须提供统一轻量任务视图
系统 MUST 统一管理转码、AI 审核、AI 标签等资源关联任务，并提供永久保留的任务列表视图，至少包含任务记录 id、资源标题、任务类型、三方任务 id、进度、状态。

#### Scenario: 查询资源任务列表
- **WHEN** 用户请求某资源的任务列表
- **THEN** 系统 SHALL 返回该资源的全部历史任务记录且不因完成状态自动清除
