## ADDED Requirements

### Requirement: 文件夹上传必须保留原始目录结构
系统 MUST 支持文件夹上传并自动识别文件类型，且在资源层保持完整目录层级关系，使用户可按原结构浏览与下载。

#### Scenario: 上传多级项目目录
- **WHEN** 用户上传包含多级子目录与多类型文件的文件夹
- **THEN** 系统 SHALL 创建对应文件夹资源树并将文件落位到原始相对路径位置
