## ADDED Requirements

### Requirement: 转码策略必须支持平台标识与栏目优先绑定
系统 MUST 支持按平台标识维护转码策略参数，且资源上传后 SHALL 优先采用栏目绑定的转码策略，并保留后续流程引擎接管扩展点。

#### Scenario: 栏目绑定策略优先
- **WHEN** 用户向已绑定转码策略的栏目上传视频资源
- **THEN** 系统 MUST 以栏目绑定策略创建转码任务而非使用全局默认策略
