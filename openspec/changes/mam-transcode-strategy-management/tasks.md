## 0. 前后端同步、契约与 MCP 联调（贯穿全程）

- [ ] 0.1 召开契约评审，冻结 `exchange` / OpenAPI **首版**（路径、DTO、**业务错误码**、分页参数），**前后端**对同一 PR 或文档基线
- [ ] 0.2 按**垂直切片**排期（见下表），**禁止**长周期「只后端、后接前端」；每切片有**可演示**的合并标准（与 `mam-transcode-delivery-sync` spec 一致）
- [ ] 0.3 维护**联调手册**（Base URL、测试账号、网关、存储/转码 mock 或沙箱、特征开关、已知问题）
- [ ] 0.4 在本地/联调机启动与 **`.cursor/mcp.json`** 中 **`chrome-browser`** 一致的 **MCP 浏览器**服务（默认 `http://localhost:12306/mcp`；**若项目端口不同须同步改配置**），并在关键路径**合并前**跑通**浏览器端**验证（与自动化测试互补）
- [ ] 0.5 每一切片在提测时执行**前后端联合验收**（接口 + 页面 + 网络/行为上报抓包**至少其一**可举证）；未过不**并行**开下一切片大改

**垂直切片与章节映射（同迭代内前后端成对完成）**

| 切片 / 能力 | 后端（章节） | 前端与联调（§10 对应项） |
|-------------|--------------|---------------------------|
| 转码策略管理 | §2 + §1 | 10.1，与 0.4 MCP 走策略页 |
| 栏目绑定 | §3 + §1 | 10.2，与 0.4 MCP 走栏目编辑 |
| 上传解析 + 主转码 | §4 | 10.3 上传、任务观察；0.4 可验证上传后列表/任务 |
| 封面 / 雪碧图 + 列表详情 | §5 + §6 | 10.3 列表/详情字段；0.4 验封面/码率展示 |
| 下载 + 行为上报 | §7 | 10.4、10.5；0.4 验下载与请求 |
| Kafka / ES 检索 | §8 | 10.3 与检索结果**一致**；**以前端可感知为准**的验收（搜索命中） |
| 交付收尾 | §9 / §11 | 文档、回归清单；0.4 **全路径** 冒烟 |

## 1. 数据模型与迁移

- [ ] 1.1 定稿 `transcode_strategy` 表字段（含 `resourceType`、`engineStrategyId`、`extraParamsJson`、`isGlobalDefault`、审计字段）及主键/租户范围
- [ ] 1.2 为「同 `resourceType` 至多一条全局默认」添加唯一约束或等价部分唯一索引，并评审与历史数据兼容性
- [ ] 1.3 定稿 `catalog_transcode_strategy_binding`（或栏目表扩展）字段与 `(catalog_id, resource_type)` 唯一约束
- [ ] 1.4 编写 Flyway/Liquibase 等迁移脚本，并在测试库执行验证
- [ ] 1.5 补充资源任务表对 `taskType`（主转码 / 封面 / 雪碧图）的枚举与索引（若沿用既有表则仅补文档与约束）

## 2. 转码策略管理（后端 + 权限；**FE 同迭代：§10.1**）

- [ ] 2.1 实现策略 exchange DTO 与 CRUD 接口（分页、按 `resourceType` 过滤）
- [ ] 2.2 实现创建/更新时全局默认互斥校验及明确业务错误码
- [ ] 2.3 实现 `extraParamsJson` 的校验策略（若采用 Schema/白名单则落配置与错误信息）
- [ ] 2.4 为管理接口配置 Sa-Token/菜单权限点，并补无权限用例
- [ ] 2.5 定义删除策略规则（被栏目引用时禁止或级联提示）并实现

## 3. 栏目与策略绑定（**FE 同迭代：§10.2**）

- [ ] 3.1 在栏目详情/编辑聚合接口中返回各 `resourceType` 下 `strategyId` 与展示用策略名称
- [ ] 3.2 实现栏目绑定保存接口，校验 `strategyId` 存在且 `resourceType` 一致
- [ ] 3.3 绑定写操作与栏目编辑权限联动，补只读栏目拒绝用例

## 4. 上传策略解析与主转码交付（**FE 同迭代：§10.3 上传**）

- [ ] 4.1 在上传完成/创建资源用例中接入解析链（显式 → 栏目 → 全局 → 不主转码），并统一「资源类型」判定来源
- [ ] 4.2 实现显式策略与资源类型不一致、显式策略不存在等错误路径
- [ ] 4.3 若存在分片上传，约定并实现显式策略仅首包/最终包唯一来源，防尾包覆盖
- [ ] 4.4 主转码任务创建时写入 `engineStrategyId` 与 `extraParamsJson`，并对接 kitty-transcoder 入参
- [ ] 4.5 未命中主转码时不创建主链路任务；任务或扩展字段记录解析来源（显式/栏目/全局）供审计
- [ ] 4.6 单测或集成测试覆盖四条解析路径与显式非法场景

## 5. 视频封面与雪碧图任务（**与 §6 同切片联调**）

- [ ] 5.1 视频在资源流程中固定创建抽封面任务，与主转码任务类型分离
- [ ] 5.2 封面成功后默认投递异步雪碧图任务，失败可重试且不影响主转码成功语义
- [ ] 5.3 列表/详情在封面就绪与主转码进行中时的字段行为与文档一致

## 6. 列表、详情与码率 DTO（**FE 同迭代：§10.3 列表/详情**）

- [ ] 6.1 扩展资源分页列表 DTO：按类型内嵌 `coverUrl`/音频预览等预览字段与就绪语义
- [ ] 6.2 扩展资源详情或码率接口：一次返回全部已就绪码率/产物元数据
- [ ] 6.3 确认旧客户端兼容（新字段可选、不破坏旧解析），并更新 API 文档
- [ ] 6.4 列表/详情仍走栏目与资源可见性校验；`open` 不绕过无权限访问

## 7. 下载与行为上报（**FE 同迭代：§10.4、§10.5**）

- [ ] 7.1 实现按 `destinationType`（或统一分级键）签发单资源下载直链（有限 TTL）
- [ ] 7.2 单资源无该码率产物时返回明确错误，不静默改源码（与 spec 一致）
- [ ] 7.3 实现下载权限校验（仅浏览不等于可下载，除非产品配置允许）
- [ ] 7.4 对接行为上报（单条）：`resourceId`、`destinationType`、标题、`operatorId`
- [ ] 7.5 批量下载：服务端支持按批生成链/打包；配合前端「无码率弹窗」契约；实现批量行为上报且逐条可区分命中/源码降级
- [ ] 7.6 下载与上报的集成测试或契约测试

## 8. Kafka 投递与 ES 自消费（**与前端检索可见性在 §0.5 联调**）

- [ ] 8.1 定稿 Topic、消息体（`resourceId`、变更类型、版本/时间戳）与生产者配置
- [ ] 8.2 在资源主数据/可检索字段事务 `afterCommit` 中投递 Kafka（无提前发送竞态）
- [ ] 8.3 实现 MAM 侧消费者：幂等写 ES、重试与可选 DLQ；乱序时以新版本为准或重查 DB
- [ ] 8.4 资源删除或不可检索终态时投递删除语义，ES 最终不可命中
- [ ] 8.5 若存在旧队列投递，制定单一路径迁移并下线双写（与 `material-search-sync` 对齐）

## 9. OpenSpec 与既有 spec 合并

- [ ] 9.1 将本变更 delta 合并入 `material-transcode-strategy`（若仓库仍存独立 spec）
- [ ] 9.2 更新 `material-search-sync`：补充事务后 Kafka + MAM 自消费职责说明
- [ ] 9.3 执行或记录 `openspec sync` 步骤与合并说明

## 10. 前端（与 **§2–§8 同迭代**；验收见 **§0**、MCP 见 **0.4**）

- [ ] 10.1 转码策略管理页：列表、表单、`resourceType` 与全局默认（互斥提示）；**与 2.x 同切片**，契约冻结后再开工
- [ ] 10.2 栏目编辑：按资源类型绑定策略与回显；**与 3.x 同切片**；**联调**时与后端共用一个环境
- [ ] 10.3 资源列表/详情：消费新预览与码率字段，移除对独立预览接口的依赖（若曾使用）；**与 6.x / 5.x 同切片**；上传页与 **§4** 同步接显式策略等参数（若有）
- [ ] 10.4 单文件与批量下载 UI：码率选择、无码率弹窗、批量完成后行为上报调用；**与 7.x 同切片**
- [ ] 10.5 前端权限与错误提示与后端业务码、**messageSource** 键**对齐**（禁止硬编码与后端冲突的「猜测」文案）
- [ ] 10.6 在**联调通过**的构建上，使用 **MCP `chrome-browser`**（见 **§0.4**）对策略页、栏目、列表、下载**至少**跑一轮**可重复**冒烟，结果记入 MR/发布说明（服务不可用时改用手写 E2E 清单，**见 `mam-transcode-delivery-sync` spec**）

## 11. 可观测、文档与收尾

- [ ] 11.1 接入 Kafka lag、封面 P95、按 `engineStrategyId` 分桶转码成功率等监控或日志规范
- [ ] 11.2 更新部署文档：Kafka Topic/消费组、下载 TTL 配置、特征开关、**MCP 联调**端口与 Cursor 配置说明
- [ ] 11.3 端到端回归清单（策略→上传→列表→下载→检索）与发布检查表；**含** §0.4 / §10.6 的 MCP 或**等价**人工路径
