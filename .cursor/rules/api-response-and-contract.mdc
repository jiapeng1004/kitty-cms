---
description: 接口正常裸响应、错误抛异常、对外 API 的 DTO/VO 与 HttpExchange 接口必须放在 -api 包，后续代码生成必须严格遵守
globs: **/*Controller.java,**/*Api.java,**/*-api/**/*.java
---

# API 响应与契约规范（强制）

本规则与 README「项目规范」一致，**所有新写或生成的代码必须严格遵守**。

## 1. 正常响应：裸返回，不包装

- 接口**成功**时，直接返回业务数据类型，**禁止**再包一层 `{ code, data, message }` 或类似统一成功壳。
- 允许的返回类型示例：`XxxVO`、`PageRespVo<XxxVO>`、`String`、`Boolean`、`List<XxxVO>` 等。
- 前端/调用方通过 **HTTP 2xx** 判断成功，通过响应体直接使用业务数据。

```java
// 正确：直接返回
@GetMapping("/api/user/{id}")
UserVO getById(String id);

@GetMapping("/api/user/query")
PageRespVo<UserVO> query(UserQueryPageDTO query);

// 禁止：成功也包一层 code/data
return Result.ok(data);  // 不要
return Map.of("code", 0, "data", data);  // 不要
```

## 2. 错误响应：抛异常，由全局异常处理包装

- **禁止**在正常返回结构里用 `code != 0` 或 `success == false` 表示业务错误。
- 业务异常必须**抛出异常**（如 `BizException`），由项目已有的 **GlobalExceptionHandler**（或等价 `@ControllerAdvice`）统一捕获，并序列化为错误响应（如 `CommonErrorResult`）。
- 调用方通过 **HTTP 非 2xx** 或约定错误体结构判断失败。

```java
// 正确：校验失败或业务失败时抛异常
if (user == null) {
    throw BizException.of(ResultStatus.USER_NOT_FOUND);
}
return userVo;

// 禁止：用返回值表示错误
return Result.fail("用户不存在");  // 不要
return Map.of("code", 404, "message", "用户不存在");  // 不要
```

## 3. 对外 API：DTO/VO 与 HttpExchange 接口必须在 -api 包

- **除明确仅用于内部、不对外暴露的接口外**，所有对外 HTTP API 均需满足：
  1. **DTO、VO** 定义在对应业务的 **`-api` 模块**（如 `kitty-user-api`、`kitty-cms-api`），**不得**放在 `-func`、`-server` 等实现模块。
  2. 在 **`-api` 模块**中定义 **Api 接口**，使用 Spring **HttpExchange** 系注解（如 `@GetMapping`、`@PostMapping`、`@PutMapping`、`@DeleteMapping`）声明路径与方法签名。
  3. **Controller** 在实现模块（如 `-func`）中 **implements** 上述 Api 接口，实现类中只做委托到 Service，不新增对外契约。

```text
kitty-user-api/    ← DTO、VO、XxxApi 接口（HttpExchange）
kitty-user-func/   ← XxxController implements XxxApi，以及 Service/实体等
```

- 入参、出参**禁止**使用 `Map`，必须使用 DTO/VO（见 `controller-request-response.mdc`）。

## 4. 与 README 的对应关系

- 本规则与仓库根目录 **README.md** 中「项目规范」一节一致。
- 代码生成与人工修改时，以本规则与 README 为准，不得出现：
  - 成功响应被统一包装成 `{ code, data }`；
  - 错误通过返回值里的 code/message 表达而不抛异常；
  - 对外 API 的 DTO/VO 或 HttpExchange 接口写在 `-func` 等非 `-api` 包中。
